name: Deploy Flask App to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Deploy to EC2 via SSH
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        script: |
          # Clean up existing containers and images
          sudo docker rm -f flask-container || true
          sudo docker rmi flask-app || true
          # Clone and deploy
          git clone https://github.com/Saifudheenpv/flask-docker-deploy || true
          cd flask-docker-deploy
          sudo docker build -t flask-app .
          sudo docker run -d -p 5000:5000 --name flask-container flask-app 